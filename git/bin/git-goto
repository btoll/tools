#!/bin/bash

if [ $# -eq 0 ]; then
    echo "Usage: $0 <TAG NAME>"
    exit 1
fi

# Allow the tag name to be passed as either just the version number or the full tag name.
[[ $1 =~ ^[[0-9].* ]] && TAG="extjs$1" || TAG=$1
git checkout $(git show $TAG | grep commit | awk '{print $2}')
